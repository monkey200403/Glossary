import React, { useState } from 'react'

export const Usestate = () => {
    // const [myname, setMyname] = useState('madhu')
    // const [myage,setMyage] = useState(0)
    // const [istrue, setIstrue] = useState(false)
    // const [numbers, setNumbers] = useState(0)

    // function decrease(params) {
    //     myage<=0 ? setMyage(0) : setMyage(myage-1)
    // }
// numbers%2==0 ? alert("even") : alert("odd")numbers%2==0 ? alert("even") : alert("odd")

  return (
    <div>
        {/* {myname}
        <button onClick={()=>setMyname("mitha")}>click</button>
        {myage}
        <button onClick={()=>setMyage(myage+1)}>+</button> */}
        {/* <button onClick={()=>myage<=0 ? setMyage(0) : setMyage(myage-1)}>-</button> */}
        {/* <button onClick={()=>decrease()}>-</button>
        {istrue ? "true" : "false"}
        <button onClick={()=>setIstrue(true)}>true</button>
        <button onClick={()=>setIstrue(false)}>false</button> */}
        {/* {numbers} */}
        {/* <button onClick={()=>setNumbers(numbers+1)}>+</button>
        <button onClick={()=>{numbers<=0 ? setNumbers(0) : setNumbers(numbers-1)}}>-</button> */}
    </div>
  )
}
